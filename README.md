# asset-playground

## Development

Mac Setup:
* brew install daemontools
* brew install node
* brew install parallel
* npm install -g mocha-phantomjs

```
source dev.env
start-assets
open http://localhost:2001
```
